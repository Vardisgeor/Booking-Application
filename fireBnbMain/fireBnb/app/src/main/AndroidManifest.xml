<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/FireBnb"
        tools:targetApi="31">
        <activity
            android:name=".activity_entrance"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/FireBnb">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity_filters"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/FireBnb">
        </activity>
        <activity
            android:name=".activity_results"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/FireBnb">

        </activity>
        <activity
            android:name=".activity_book"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/FireBnb">

        </activity>
        <activity
            android:name=".activity_complete"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/FireBnb">

        </activity>


    </application>

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permission to read from external storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Permission to write to external storage -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Permission to access location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Permission to access coarse location -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Permission to record audio -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Permission to use the device's microphone -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

</manifest>